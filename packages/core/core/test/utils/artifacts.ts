// Artifact writer utilities for parity tests.
// TODO: implement in Epic 0. Suggested API shape:
// - writeSummary({engine, fixture, bundles, graphs, diagnostics, timings}, outPath)
// - writeEvents(events: unknown[], outPath)
// Use normalize helpers to sanitize outputs before writing.
export {};
