# Ticket: Opt-In/Out Documentation and CLI Help

## Objective

Document how to enable/disable the Rust engine, interpret parity diffs, and use fallback behavior.

## Tasks

- Update docs (index/CLI help) with flag descriptions, expected outputs, and troubleshooting steps.
- Provide guidance for dual-run mode, artifact locations, and how to compare results.
- Add troubleshooting for missing native binary/WASM path and automatic JS fallback messaging.

## Acceptance Criteria

- Docs published and linked from CLI `--help` and migration index.
- Users can self-serve enable/disable and understand parity output locations.
- Troubleshooting covers common errors and fallback behavior.
